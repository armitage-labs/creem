---
title: 'Create a webhook endpoint'
description: 'Register a URL to receive event deliveries. The response includes the signing secret; store it, it is the only time the create call returns it.'
openapi: post /v1/webhooks
---
