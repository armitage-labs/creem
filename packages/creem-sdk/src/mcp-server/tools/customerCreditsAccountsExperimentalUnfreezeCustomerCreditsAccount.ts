/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { customerCreditsAccountsExperimentalUnfreezeCustomerCreditsAccount } from "../../funcs/customerCreditsAccountsExperimentalUnfreezeCustomerCreditsAccount.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  id: z.string(),
};

export const tool$customerCreditsAccountsExperimentalUnfreezeCustomerCreditsAccount:
  ToolDefinition<typeof args> = {
    name:
      "customer-credits-accounts-(experimental)-unfreeze-customer-credits-account",
    description: `Unfreeze an account

Unfreeze a frozen account to allow transactions again.`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] =
        await customerCreditsAccountsExperimentalUnfreezeCustomerCreditsAccount(
          client,
          args.id,
          { fetchOptions: { signal: ctx.signal } },
        ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
