---
"creem": minor
"@creem_io/cli": minor
---

Add the usage-based billing surface: new `events` group (`ingestEvents` batch usage ingestion, `listEvents`) and `meters` group (create, list, get, update, preview, preview-stored, per-customer consumed units, archive, unarchive) in the SDK, with matching `creem events …` and `creem meters …` CLI commands. Customer-credits operations drop their experimental marker.
